services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: smart-attendance:latest
    container_name: smart_attendance_web
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - ALLOWED_HOSTS=*
      - SECRET_KEY=django-insecure-docker-smart-attendance-key
